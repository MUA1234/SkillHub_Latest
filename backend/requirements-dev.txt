# Dev/test-only dependencies (Phase 0 test suite). Not installed in prod.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
respx>=0.21          # httpx mock transport, for testing the async client over a fake DB
